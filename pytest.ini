[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --cov=agents
    --cov=orchestration
    --cov=infrastructure
    --cov-report=html
    --cov-report=term-missing
